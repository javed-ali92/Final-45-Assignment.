// storing the famous quote and its author

let quote: string = "Nothing is imposible because imposible itself I M POSSIBLE"

let author: string = "Javed Ali";

//printing the message 

console.log(`"${quote}" - ${author}`);